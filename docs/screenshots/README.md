# Screenshots

Drop 4 PNGs here before submitting (see the HTML comment in the main `README.md`'s
"Screenshots" section for exact names/sizes and what each one should show):

- `fleet.png`
- `vault.png`
- `burn.png`
- `mcp-manifest.png`

They're referenced directly by path from the root `README.md` and will render there
automatically once added -- no other edit needed.
